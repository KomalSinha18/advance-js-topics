# JavaScript Closures Practice

This project contains my practice code for understanding **JavaScript Closures**, including:

- Closure basics
- Closure with `setTimeout`
- Loop behavior using `var` vs `let`
- How closures work with event listeners and delayed execution

## Files

- `closures.js`: Contains all the example problems and solutions
- `index.html` (optional): For testing JS in the browser
- `README.md`: You’re reading it now 😊

## Topics Covered

✅ Function scopes  
✅ Lexical environment  
✅ Closure behavior in loops  
✅ Fixing `var` issues using helper functions  
✅ Interview patterns using closures
